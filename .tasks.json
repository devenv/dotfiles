{
    "tasks": [
        {
            "name": "Rebase from main",
            "cmd": "git fetch --all && git rebase origin/main -i"
        },
        {
            "name": "Install debug pips",
            "cmd": "pip install neovim debugpy pdbpp jurigged black pylsp-rope python-language-server python-lsp-black python-lsp-server docker pytest-pyright"
        }
    ]
}
